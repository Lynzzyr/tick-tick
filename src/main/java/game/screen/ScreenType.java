package game.screen;

/** All screen types. */
public enum ScreenType {
    SPLASH, HOME, SELECTION,
    LEVEL01, LEVEL02, LEVEL03, LEVEL04, LEVEL05,
    LEVEL06, LEVEL07, LEVEL08, LEVEL09, LEVEL10,
    END
}